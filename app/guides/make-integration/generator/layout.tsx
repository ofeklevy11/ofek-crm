import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "מחולל בקשות POST | BizlyCRM",
};

export default function GeneratorLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
