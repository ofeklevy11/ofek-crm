# === DATABASE (used by docker-compose to create the DB) ===
POSTGRES_USER="crm_user"
POSTGRES_PASSWORD="CHANGE_ME_STRONG_PASSWORD"
POSTGRES_DB="ofek_crm"

# === REDIS ===
REDIS_PASSWORD="GENERATE_WITH: openssl rand -hex 24"

# DATABASE_URL is set automatically by docker-compose.production.yml
# REDIS_URL is set automatically by docker-compose.production.yml
# INNGEST_DEV is set automatically by docker-compose.production.yml
# INNGEST_BASE_URL is set automatically by docker-compose.production.yml

# === APP ===
NODE_ENV="production"
NEXT_PUBLIC_APP_URL="http://YOUR_VPS_IP"
SESSION_SECRET="GENERATE_WITH: openssl rand -hex 32"
CRON_SECRET="GENERATE_WITH: openssl rand -hex 16"

# === UPLOADTHING (keep your existing token) ===
UPLOADTHING_TOKEN=""

# === OPENROUTER (AI) ===
OPENROUTER_API_KEY=""

# === PDF GENERATION ===
PDFMONKEY_TEMPLATE_ID=""
PDFMONKEY_API_KEY=""

# === WHATSAPP ===
WHATSAPP_APP_ID=""
NEXT_PUBLIC_WHATSAPP_APP_ID=""
WHATSAPP_APP_SECRET=""
WHATSAPP_TOKEN_ENCRYPTION_KEY=""
NEXT_PUBLIC_WHATSAPP_CONFIG_ID=""
WHATSAPP_VERIFY_TOKEN=""
WHATSAPP_ACCESS_TOKEN=""

# === EMAIL ===
RESEND_API_KEY=""

# === MONITORING ===
TELEGRAM_BOT_TOKEN="GET_FROM_BOTFATHER"
TELEGRAM_CHAT_ID="YOUR_CHAT_OR_GROUP_ID"
WEBHOOK_SECRET="GENERATE_WITH: openssl rand -hex 16"
GF_SECURITY_ADMIN_PASSWORD="CHANGE_ME_GRAFANA_PASSWORD"

# === SECRET ROTATION TRACKING ===
# Update these dates after rotating each secret (see docs/SECRET-ROTATION.md)
# SESSION_SECRET_ROTATED_AT=YYYY-MM-DD
# POSTGRES_PASSWORD_ROTATED_AT=YYYY-MM-DD
# REDIS_PASSWORD_ROTATED_AT=YYYY-MM-DD
# CRON_SECRET_ROTATED_AT=YYYY-MM-DD
# GF_SECURITY_ADMIN_PASSWORD_ROTATED_AT=YYYY-MM-DD
# WEBHOOK_SECRET_ROTATED_AT=YYYY-MM-DD
